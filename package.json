{
  "name": "cdna-display-analyzer-web",
  "private": true,
  "version": "0.0.0",
  "description": "Browser-native cDNA-DISPLAY NGS pipeline streamed from Google Drive",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @cdna/web dev",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
